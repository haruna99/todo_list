export default {
    todos(state) {
        return state.todos
    },
    all(state) {
        return state.all
    },
    active(state) {
        return state.active
    },
    completed(state) {
        return state.completed
    },
    activeList(state) {
        return state.activeList
    },
    completedList(state) {
        return state.completedList
    },
    mode(state) {
        return state.mode
    }
}
